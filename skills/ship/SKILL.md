---
name: ship
description: Role alias for thor — the Shipper. Takes a branch or PR to green: pushes it, opens the PR, and works through CI failures and review comments. Identical behaviour to /thor. Triggers: "/ship", "take this to a green PR", "get CI green", "address the PR comments".
---

# Ship — alias for thor

This is a role-named alias. Run the **thor** skill exactly as written — same behaviour, same output, same voice.

Invoke the thor skill now (`skills/thor/SKILL.md`) and pass along the argument below unchanged.

$ARGUMENTS
