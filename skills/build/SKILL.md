---
name: build
description: Role alias for hulk — the Builder. Builds a planned feature end to end, reviews its own work until clean, verifies it runs, and opens a green PR. Identical behaviour to /hulk. Triggers: "/build <plan>", "build this and review it until clean", "autonomously implement this feature".
---

# Build — alias for hulk

This is a role-named alias. Run the **hulk** skill exactly as written — same behaviour, same output, same voice.

Invoke the hulk skill now (`skills/hulk/SKILL.md`) and pass along the argument below unchanged.

$ARGUMENTS
