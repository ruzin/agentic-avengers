---
name: spec
description: Role alias for ironman — the Spec Cutter. Turns a rough idea into a clear spec by asking the right questions, then hands it to plan mode. Identical behaviour to /ironman. Triggers: "/spec", "spec this out", "help me write a spec", "turn this into a spec for plan mode".
---

# Spec — alias for ironman

This is a role-named alias. Run the **ironman** skill exactly as written — same behaviour, same output, same voice.

Invoke the ironman skill now (`skills/ironman/SKILL.md`) and pass along the argument below unchanged.

$ARGUMENTS
