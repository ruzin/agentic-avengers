---
name: architect
description: Role alias for loki — the Architect. Explores the codebase and writes MECHANICS.md, a living map of how it all works. Identical behaviour to /loki. Triggers: "/architect", "chart the codebase", "map the architecture", "write MECHANICS.md".
allowed-tools: Agent, Read, Write, Edit, Glob, Grep, Bash
---

# Architect — alias for loki

This is a role-named alias. Run the **loki** skill exactly as written — same behaviour, same output, same voice.

Invoke the loki skill now (`skills/loki/SKILL.md`) and pass along the argument below unchanged.

$ARGUMENTS
